{
  "name": "task_4",
  "version": "1.0.0",
  "description": "TypeScript namespace and declaration merging task",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  }
}
